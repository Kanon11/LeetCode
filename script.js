let log100 = (x) => {
    return Math.log(x) / Math.log(100);
}
let sum = (x, y) => {
    return x + y;
}
// module.exports = { log100,sum };

// export default sum;
export { sum, log100 };
// export {sum,log100}
